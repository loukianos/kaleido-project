{
  "name": "kaleido-contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:besu": "hardhat ignition deploy ignition/modules/LoanNote.ts --network besu --reset",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.2.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "@types/node": "^22.10.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.14.0",
    "hardhat": "^3.9.0",
    "typescript": "^5.7.2"
  }
}
